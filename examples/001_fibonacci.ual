-- 01: Fibonacci - Generate first N fibonacci numbers

var n i64 = 20
var a i64 = 0
var b i64 = 1

@fib = stack.new(i64)
@fib push:a

var i i64 = 1
while (i < n) {
    @fib push:b
    push:a push:b add let:temp
    push:b let:a
    push:temp let:b
    push:i inc let:i
}
var temp i64 = 0

-- Output last value
@fib peek dot
